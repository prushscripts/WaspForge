(*
  WaspForge — list nearby RSObjects via map JSON within a tile radius.
  Part of the WaspForge script factory system.
*)
{$DEFINE WF_GRAB_OBJECTS_INCLUDED}
{$IFNDEF WF_TYPES_INCLUDED}
  {$I ../core/types.simba}
{$ENDIF}

function GrabObjectsJSON(): String;
begin
  // Placeholder bridge payload until full object scanner is wired to web UI.
  Result := '[]';
end;

