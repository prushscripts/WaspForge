(*
  WaspForge — list nearby RSEntities via map JSON within a tile radius.
  Part of the WaspForge script factory system.
*)
{$DEFINE WF_GRAB_NPCS_INCLUDED}
{$IFNDEF WF_TYPES_INCLUDED}
  {$I ../core/types.simba}
{$ENDIF}

function GrabNPCsJSON(): String;
begin
  // Placeholder bridge payload until full NPC scanner is wired to web UI.
  Result := '[]';
end;

